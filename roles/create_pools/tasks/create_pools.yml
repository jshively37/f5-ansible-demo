---
- name: Create A Pool
  bigip_pool:
    provider: "{{ provider }}"
    state: present
    partition: Common
    lb_method: ratio-member
    name: Jeff-Web03
    slow_ramp_time: 120
    validate_certs: False